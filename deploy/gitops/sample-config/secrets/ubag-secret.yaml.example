# External secret reference — replace with your actual secret provider
# (ExternalSecrets Operator, Vault, etc.)
# Do NOT commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: ubag-secret
  namespace: ubag
type: Opaque
stringData:
  UBAG_APP_SECRET: "REPLACE_ME"
  UBAG_POSTGRES_DSN: "REPLACE_ME"
