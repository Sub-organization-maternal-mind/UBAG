{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the UBAG mobile monitor: core IPC plus outbound HTTP to the user-configured gateway. The HTTP scope is intentionally broad because the gateway URL is operator-supplied at runtime; tighten it to your fleet's hosts in production.",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux", "iOS", "android"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "http://*:*" },
        { "url": "https://*:*" },
        { "url": "http://*" },
        { "url": "https://*" }
      ]
    }
  ]
}
